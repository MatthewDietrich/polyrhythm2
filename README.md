# Polyrhythm Generator

An app that creates a polyrhythmic pattern given a list of notes.

Usage: Use pip or uv to install dependencies, then run main.py

## Configuration
Use config.yaml to customize various parameters including
- Window size
- Colors
- Speed
- Notes to play